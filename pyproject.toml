[project]
name = "Zetin-Humanoid-RL"
version = "0.1.0"
description = "Reinforcement learning repository for UOS Zetin's bipedal and humanoid robots."
readme = "README.md"
requires-python = "~=3.11.0"
dependencies = []
